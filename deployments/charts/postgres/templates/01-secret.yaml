apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-postgres-secret
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  POSTGRES_DB: {{ .Values.secret.database | quote }}
  POSTGRES_USER: {{ .Values.secret.username | quote }}
  POSTGRES_PASSWORD: {{ .Values.secret.password | quote }}
