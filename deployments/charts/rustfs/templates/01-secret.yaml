apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-rustfs-secret
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  RUSTFS_ROOT_USER: {{ .Values.secret.accessKey | quote }}
  RUSTFS_ROOT_PASSWORD: {{ .Values.secret.secretKey | quote }}
