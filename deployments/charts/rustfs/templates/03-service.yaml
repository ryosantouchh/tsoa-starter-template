apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-rustfs-headless
  namespace: {{ .Values.namespace }}
spec:
  clusterIP: None
  selector:
    app: {{ .Release.Name }}-rustfs
  ports:
    - name: api
      port: {{ .Values.rustfs.port }}
      targetPort: {{ .Values.rustfs.port }}
    - name: console
      port: {{ .Values.rustfs.consolePort }}
      targetPort: {{ .Values.rustfs.consolePort }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-rustfs-svc
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Release.Name }}-rustfs
  ports:
    - name: api
      port: {{ .Values.rustfs.port }}
      targetPort: {{ .Values.rustfs.port }}
    - name: console
      port: {{ .Values.rustfs.consolePort }}
      targetPort: {{ .Values.rustfs.consolePort }}
  type: ClusterIP
