apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Release.Name }}-rustfs
  namespace: {{ .Values.namespace }}
spec:
  serviceName: {{ .Release.Name }}-rustfs-headless
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}-rustfs
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-rustfs
    spec:
      terminationGracePeriodSeconds: 30
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: rustfs
          image: {{ .Values.image.repository }}:{{ .Values.image.tag | default "latest" }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.port }}
              name: api
            - containerPort: {{ .Values.consolePort }}
              name: console
          env:
            - name: RUSTFS_ROOT_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-rustfs-secret
                  key: RUSTFS_ROOT_USER
            - name: RUSTFS_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-rustfs-secret
                  key: RUSTFS_ROOT_PASSWORD
            - name: RUSTFS_VOLUMES
              value: "/data"
            - name: RUSTFS_ADDRESS
              value: ":{{ .Values.port }}"
            - name: RUSTFS_CONSOLE_ADDRESS
              value: ":{{ .Values.consolePort }}"
          volumeMounts:
            - name: data
              mountPath: /data
          # livenessProbe:
          #   httpGet:
          #     path: /minio/health/live
          #     port: api
          #   initialDelaySeconds: 30
          #   periodSeconds: 20
          #   failureThreshold: 3
          # readinessProbe:
          #   httpGet:
          #     path: /minio/health/ready
          #     port: api
          #   initialDelaySeconds: 10
          #   periodSeconds: 10
          #   failureThreshold: 3
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: {{ .Values.persistence.size }}
        {{- if and .Values.persistence.storageClass (ne .Values.persistence.storageClass "") }}
        storageClassName: {{ .Values.persistence.storageClass }}
        {{- end }}
