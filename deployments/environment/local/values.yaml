global:
  namespace: api-app-local-namespace
  environment: development

api-app:
  namespace: api-app-local-namespace
  environment: development
  image:
    tag: dev
    pullPolicy: Always
  ingress:
    enabled: true
    host: api.localhost
  config:
    NODE_ENV: development

postgres:
  namespace: api-app-local-namespace

rustfs:
  namespace: api-app-local-namespace
